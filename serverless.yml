service: my-express-application

provider:
  name: aws
  runtime: nodejs14.x
  stage: ${self:custom.stage}
  region: us-east-1

functions:
  app:
    handler: index.handler
    events:
      - http: ANY /
      - http: "ANY {proxy+}"

custom:
  stage: "${opt:stage, 'dev'}"
