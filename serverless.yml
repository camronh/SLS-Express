service: my-express-application

provider:
  name: aws
  runtime: nodejs14.x
  stage: ${self:custom.stage}
  environment:
    API_KEY: ${ env.API_KEY }

functions:
  app:
    handler: index.handler
    events:
      - http: ANY /
      - http: "ANY {proxy+}"

custom:
  stage: "${opt:stage, 'dev'}"
